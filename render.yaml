services:
  - type: web
    name: citypulse-alamein
    runtime: node
    plan: free
    region: frankfurt
    branch: main
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /api/dashboard/city-overview
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
